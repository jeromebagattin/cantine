{# src/CAF/CantineBundle/Resources/views/TypePlats/index.html.twig #}

{#
        <h1>id plat : {{typeplat.id}} - Type de plats : {{ typeplat.libelle }}</h1>
        <ul>
            {% for plat in typeplat.plats %}
                <li>{{ plat.id }} - {{ plat.libelle }}</li>
            {% endfor %}
        </ul>#}
        
    

{% extends "CAFCantineBundle::cantine.html.twig" %}

{% block title %}
  Type de plats - {{ parent() }}
{% endblock %}

{% block cafcantine_body %}
    <ul class="nav nav-pills nav-stacked">
        {% for typeplat in typeplats %}
            <li>
                <a href="{{ path('caf_typeplats_edit', {id:typeplat.id}) }}">
                    {{ typeplat.libelle}}
                </a>  
            </li>
        {% endfor %}
    </ul>
      <a href="{{ path('caf_typeplats_add') }}"> + </a>  
    
{% endblock %}